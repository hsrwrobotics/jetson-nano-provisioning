[project]
name = "robotics-lab-turtlebots"
version = "0.1.0"
description = "Provision Jetson Nano TurtleBot 1 boards: headless Ubuntu 20.04 (focal) + Docker (tailscale, kobuki ROS 2 Humble, rosbridge)."
readme = "README.md"
requires-python = ">=3.8,<3.9"
dependencies = [
    "ansible-core==2.13.13",
    "cryptography<47",
]
